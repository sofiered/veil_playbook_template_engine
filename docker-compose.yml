services:
  templater:
    build: .
    entrypoint: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "${port}", "--proxy-headers"]
    env_file:
      - .env
    ports:
      - 8000:8000
    volumes:
      - ${data_path}:/app/data